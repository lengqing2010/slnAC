テーブル定義書(ARIMA用_週次実績差分データIFテーブル).xls.⇒ＡＲＩＭＡ実績データIFテーブル:OK

テーブル定義書(EDIエラーデータテーブル).xls.⇒EDIエラーデータテーブル:OK

テーブル定義書(EDI出荷予定IFテーブル（欠品明細）).xls.⇒EDI出荷予定IFテーブル（欠品明細）:OK

テーブル定義書(EDI出荷予定IFテーブル（納品明細）).xls.⇒EDI出荷予定IFテーブル（納品明細）:OK

テーブル定義書(EDI出荷予定IFテーブル（納品梱包）).xls.⇒EDI出荷予定IFテーブル（納品梱包）:OK

テーブル定義書(EDI出荷予定データテーブル（欠品明細）).xls.⇒EDI出荷予定データテーブル（欠品明細）:OK

テーブル定義書(EDI出荷予定データテーブル（納品明細）).xls.⇒EDI出荷予定データテーブル（納品明細）:OK

テーブル定義書(EDI出荷予定データテーブル（納品梱包）).xls.⇒EDI出荷予定データテーブル（納品梱包）:OK

テーブル定義書(EDI納品事故データテーブル).xls.⇒EDI納品事故データテーブル:OK

テーブル定義書(EDI納品梱包データテーブル).xls.⇒EDI納品梱包データテーブル:OK

テーブル定義書(HT店振受信ワークテーブル).xls.⇒HT店振受信ワークテーブル:OK

テーブル定義書(HT検品データテーブル).xls.⇒HT検品データテーブル:OK

テーブル定義書(HT検品受信ワークテーブル).xls.⇒HT検品受信ワークテーブル:OK

テーブル定義書(HT検品複数伝票情報テーブル).xls.⇒HT検品複数伝票情報テーブル:OK

テーブル定義書(JAN出力予約テーブル).xls.⇒JAN出力予約テーブル:OK

テーブル定義書(JAN自社特売変換マスタIFテーブル).xls.⇒JAN自社特売変換マスタIFテーブル:OK

テーブル定義書(PC出力スプールテーブル).xls.⇒PC出力スプールテーブル:OK

テーブル定義書(PC出力予約テーブル).xls.⇒PC出力予約テーブル:OK
テーブル定義書(PC出力予約テーブル).xls.⇒×ＰＣ出力予約_bk_20120702:OK
テーブル定義書(PC出力予約テーブル).xls.⇒×ＰＣ出力予約 bk:OK

テーブル定義書(POP出力予約テーブル).xls.⇒POP出力予約テーブル:OK

テーブル定義書(POSメンテ起動制御テーブル).xls.⇒POSメンテ起動制御テーブル:OK

テーブル定義書(POS伝送_クラスメンテナンスIFテーブル（データ部）).xls.⇒POS伝送_クラスメンテナンスIFテーブル（データ部）:OK

テーブル定義書(POS伝送_クラスメンテナンスIFテーブル（ヘッダ部）).xls.⇒POS伝送_クラスメンテナンスIFテーブル（ヘッダ部）:OK

テーブル定義書(POS伝送_単品メンテナンスIF(即時)テーブル（データ部）).xls.⇒POS伝送_単品メンテナンスIF(即時)テーブル（データ部）:OK

テーブル定義書(POS伝送_単品メンテナンスIF(即時)テーブル（ヘッダ部）).xls.⇒POS伝送_単品メンテナンスIF(即時)テーブル（ヘッダ部）:OK

テーブル定義書(POS伝送_単品メンテナンスIFテーブル（データ部）).xls.⇒POS伝送_単品メンテナンスIFテーブル（データ部）:OK

テーブル定義書(POS伝送_単品メンテナンスIFテーブル（ヘッダ部）).xls.⇒POS伝送_単品メンテナンスIFテーブル（ヘッダ部）:OK

テーブル定義書(WX単品マスタワークテーブル).xls.⇒WX単品マスタワークテーブル:OK

テーブル定義書(WX更新メンテテーブル).xls.⇒WX更新メンテテーブル:OK

テーブル定義書(WX送信店データテーブル).xls.⇒WX送信店データテーブル:OK

テーブル定義書(おはよう帳票出力制御テーブル).xls.⇒おはよう帳票出力制御テーブル:OK

テーブル定義書(おはよう帳票出力起動制御テーブル).xls.⇒おはよう帳票出力起動制御テーブル:OK

テーブル定義書(まる得商品IFテーブル).xls.⇒まる得商品IFテーブル:OK

テーブル定義書(まる得商品テーブル（当月）).xls.⇒まる得商品テーブル（当月）:OK

テーブル定義書(まる得商品テーブル（翌月）).xls.⇒まる得商品テーブル（翌月）:OK

テーブル定義書(アンマッチデータIFテーブル).xls.⇒アンマッチデータIFテーブル:OK

テーブル定義書(アンマッチデータテーブル).xls.⇒アンマッチデータテーブル:OK

テーブル定義書(クラスコードIFテーブル).xls.⇒クラスコードIFテーブル:OK

テーブル定義書(クラス別売上精算テーブル).xls.⇒クラス別売上精算テーブル:OK

テーブル定義書(クラス清算IFテーブル).xls.⇒クラス清算IFテーブル:OK

テーブル定義書(クラブ買物券データテーブル).xls.⇒クラブ買物券データテーブル:OK

テーブル定義書(クラブ買物券明細IFテーブル).xls.⇒クラブ買物券明細IFテーブル:OK

テーブル定義書(ゴンドラシール出力予約テーブル).xls.⇒ゴンドラシール出力予約テーブル:OK

テーブル定義書(ジョブ進捗状況テーブル).xls.⇒ジョブ進捗状況テーブル:OK

テーブル定義書(スキャンデータ取込IFテーブル).xls.⇒スキャンデータ取込IFテーブル:OK

テーブル定義書(スキャンデータ取込テーブル).xls.⇒スキャンデータ取込テーブル:OK

テーブル定義書(スキャンデータ取込ワークテーブル).xls.⇒スキャンデータ取込ワークテーブル:OK

テーブル定義書(スキャン集計データ取込IFテーブル).xls.⇒スキャン集計データ取込IFテーブル:OK

テーブル定義書(スキャン集計データ取込テーブル).xls.⇒スキャン集計データ取込テーブル:OK

テーブル定義書(スキャン集計データ取込ワークテーブル).xls.⇒スキャン集計データ取込ワークテーブル:OK

テーブル定義書(スピード検品専用印刷制御テーブル).xls.⇒スピード検品専用印刷制御テーブル:OK

テーブル定義書(ダイレクトメンテ結果テーブル).xls.⇒ダイレクトメンテ結果テーブル:OK

テーブル定義書(ダイレクトメンテ結果テーブル（累積）).xls.⇒ダイレクトメンテ結果テーブル（累積）:OK

テーブル定義書(ダミー仕入テーブル).xls.⇒ダミー仕入テーブル:OK

テーブル定義書(バッチエラー情報テーブル).xls.⇒バッチエラー情報テーブル:OK

テーブル定義書(ランク別欠品集計テーブル).xls.⇒ランク別欠品集計テーブル:OK

テーブル定義書(リテール取引先IFテーブル).xls.⇒リテール取引先IFテーブル:OK

テーブル定義書(リテール商品IFテーブル).xls.⇒リテール商品IFテーブル:OK

テーブル定義書(リテール商品テーブル).xls.⇒リテール商品テーブル:OK

テーブル定義書(レジ別売上デ－タテーブル（売場別）).xls.⇒レジ別売上デ－タテーブル（売場別）:OK

テーブル定義書(レジ別売上デ－タテーブル（大分類別）).xls.⇒レジ別売上デ－タテーブル（大分類別）:OK

テーブル定義書(レジ別売上デ－タテーブル（店舗別）).xls.⇒レジ別売上デ－タテーブル（店舗別）:OK

テーブル定義書(レジ売価変更データテーブル).xls.⇒レジ売価変更データテーブル:OK

テーブル定義書(ロスIFテーブル).xls.⇒ロスIFテーブル:OK

テーブル定義書(ロステーブル).xls.⇒ロステーブル:OK
テーブル定義書(ロステーブル).xls.⇒ロステーブル (古):NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_los, 2)。
语句已终止。

テーブル定義書(ロス調査集計GP品IFテーブル).xls.⇒ロス調査集計GP品IFテーブル:OK

テーブル定義書(ロス調査集計GP品テーブル).xls.⇒ロス調査集計GP品テーブル:OK

テーブル定義書(一時テーブル).xls.⇒一時テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_tempwork, 3)。
语句已终止。

テーブル定義書(中分類データIFテーブル).xls.⇒中分類データIFテーブル:OK

テーブル定義書(事前出荷集計テーブル).xls.⇒事前出荷集計テーブル:OK

テーブル定義書(事業所マスタIFテーブル).xls.⇒事業所マスタIFテーブル:OK

テーブル定義書(仕入伝票テーブル).xls.⇒仕入伝票テーブル:OK

テーブル定義書(仕訳IFテーブル).xls.⇒仕訳IFテーブル:OK

テーブル定義書(仕訳テーブル).xls.⇒仕訳テーブル:OK

テーブル定義書(仕訳ワークテーブル).xls.⇒仕訳ワークテーブル:OK

テーブル定義書(代替品自動発注(履歴)テーブル).xls.⇒代替品自動発注(履歴)テーブル:OK

テーブル定義書(代替品自動発注(当日)テーブル).xls.⇒代替品自動発注(当日)テーブル:OK

テーブル定義書(代替品自動発注テーブル).xls.⇒代替品自動発注テーブル:OK

テーブル定義書(仮仕入伝票テーブル).xls.⇒仮仕入伝票テーブル:OK

テーブル定義書(伝票印刷データテーブル).xls.⇒伝票印刷データテーブル:OK

テーブル定義書(伝種別集計テーブル).xls.⇒伝種別集計テーブル:OK

テーブル定義書(倉庫在庫テーブル).xls.⇒倉庫在庫テーブル:OK

テーブル定義書(値付変更指示テーブル).xls.⇒値付変更指示テーブル:OK

テーブル定義書(値引伝票テーブル).xls.⇒値引伝票テーブル:OK
テーブル定義書(値引伝票テーブル).xls.⇒値引伝票テーブル (旧):OK

テーブル定義書(値引理由IFテーブル).xls.⇒値引理由IFテーブル:OK

テーブル定義書(値引発行出力予約テーブル).xls.⇒値引発行出力予約テーブル:OK

テーブル定義書(値引発行出力予約履歴テーブル).xls.⇒値引発行出力予約履歴テーブル:OK

テーブル定義書(入荷予定データテーブル).xls.⇒入荷予定データテーブル:OK

テーブル定義書(入金機データテーブル（レジ別）).xls.⇒入金機データテーブル（レジ別）:OK

テーブル定義書(入金機データテーブル（店舗別）).xls.⇒入金機データテーブル（店舗別）:OK

テーブル定義書(出荷伝票IFテーブル).xls.⇒出荷伝票IFテーブル:OK

テーブル定義書(出荷伝票データテーブル).xls.⇒出荷伝票データテーブル:OK

テーブル定義書(削除情報テーブル).xls.⇒削除情報テーブル:OK
テーブル定義書(削除情報テーブル).xls.⇒×削除情報テーブル (2):OK

テーブル定義書(前日発注予定テーブル).xls.⇒前日発注予定テーブル:OK

テーブル定義書(単品マスタリカバリー情報取込エラーテーブル).xls.⇒単品マスタリカバリー情報取込エラーテーブル:OK

テーブル定義書(単品マスタリカバリー情報取込ワークテーブル).xls.⇒単品マスタリカバリー情報取込ワークテーブル:OK

テーブル定義書(単品売上テーブル).xls.⇒単品売上テーブル:OK

テーブル定義書(単品清算IFテーブル).xls.⇒単品清算IFテーブル:OK

テーブル定義書(単品点検テーブル).xls.⇒単品点検テーブル:OK

テーブル定義書(単品点検ワークテーブル).xls.⇒単品点検ワークテーブル:OK

テーブル定義書(印刷データテーブル).xls.⇒印刷データテーブル:OK

テーブル定義書(印刷データテーブル２).xls.⇒印刷データテーブル２:OK

テーブル定義書(原価変更IFテーブル).xls.⇒原価変更IFテーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_genka_henkou_if, 6)。
语句已终止。

テーブル定義書(原価変更全件IFテーブル).xls.⇒原価変更全件IFテーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_genka_henkou_all_if, 6)。
语句已终止。

テーブル定義書(取引先マスタIFテーブル).xls.⇒取引先マスタIFテーブル:OK

テーブル定義書(取引別_MMテーブル).xls.⇒取引別_MMテーブル:OK

テーブル定義書(取引別_アイテムテーブル).xls.⇒取引別_アイテムテーブル:OK

テーブル定義書(取引別_アイテム履歴テーブル).xls.⇒取引別_アイテム履歴テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_torihikibetu_item_rireki, 202)。
语句已终止。

テーブル定義書(取引別_クレジットテーブル).xls.⇒取引別_クレジットテーブル:OK

テーブル定義書(取引別_ポイントテーブル).xls.⇒取引別_ポイントテーブル:OK

テーブル定義書(取引別_取引テーブル).xls.⇒取引別_取引テーブル:OK

テーブル定義書(取引別_取引履歴テーブル).xls.⇒取引別_取引履歴テーブル:OK

テーブル定義書(取引別_締めテーブル).xls.⇒取引別_締めテーブル:OK

テーブル定義書(取引別登録一時テーブル).xls.⇒取引別登録一時テーブル:OK

テーブル定義書(品出しデータテーブル).xls.⇒品出しデータテーブル:OK

テーブル定義書(商品コード管理エラーテーブル).xls.⇒商品コード管理エラーテーブル:OK

テーブル定義書(商品コード管理パターンテーブル).xls.⇒商品コード管理パターンテーブル:OK

テーブル定義書(商品店別エラーテーブル).xls.⇒商品店別エラーテーブル:OK

テーブル定義書(商品情報IFテーブル).xls.⇒商品情報IFテーブル:OK

テーブル定義書(商品振替テーブル(入力)).xls.⇒商品振替テーブル(入力):OK

テーブル定義書(商品振替テーブル(結果)).xls.⇒商品振替テーブル(結果):OK

テーブル定義書(商品集計情報).xls.⇒商品集計情報:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(基本棚割りテーブル).xls.⇒基本棚割りテーブル:OK

テーブル定義書(基本棚割りマスタIFテーブル).xls.⇒基本棚割りマスタIFテーブル:OK

テーブル定義書(基本棚割りマスタ明細IFテーブル).xls.⇒基本棚割りマスタ明細IFテーブル:OK

テーブル定義書(基本棚割り明細テーブル).xls.⇒基本棚割り明細テーブル:OK

テーブル定義書(売上値引管理IFテーブル).xls.⇒売上値引管理IFテーブル:OK

テーブル定義書(売上入力順テーブル).xls.⇒売上入力順テーブル:OK

テーブル定義書(売上報告(割引)IFテーブル).xls.⇒売上報告(割引)IFテーブル:OK

テーブル定義書(売上報告(売場別売上)IFテーブル).xls.⇒売上報告(売場別売上)IFテーブル:OK

テーブル定義書(売上報告(掛情報)IFテーブル).xls.⇒売上報告(掛情報)IFテーブル:OK

テーブル定義書(売上報告(部門別売上)IFテーブル).xls.⇒売上報告(部門別売上)IFテーブル:OK

テーブル定義書(売上報告_割引テーブル).xls.⇒売上報告_割引テーブル:OK

テーブル定義書(売上報告_商品券テーブル).xls.⇒売上報告_商品券テーブル:OK

テーブル定義書(売上報告_売場別売上げテーブル).xls.⇒売上報告_売場別売上げテーブル:OK

テーブル定義書(売上報告_売掛明細テーブル).xls.⇒売上報告_売掛明細テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_urikake_meisai, 14)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_urikake_meisai, 14)。
语句已终止。
语句已终止。

テーブル定義書(売上報告_売掛金照会テーブル).xls.⇒売上報告_売掛金照会テーブル:OK

テーブル定義書(売上報告_部門別売上げテーブル).xls.⇒売上報告_部門別売上げテーブル:OK

テーブル定義書(売上報告テーブル).xls.⇒売上報告テーブル:OK

テーブル定義書(売価インデックスIFテーブル).xls.⇒売価インデックスIFテーブル:OK

テーブル定義書(売価変更IFテーブル).xls.⇒売価変更IFテーブル:OK

テーブル定義書(売価変更テーブル).xls.⇒売価変更テーブル:OK

テーブル定義書(売価変更データ異常テーブル).xls.⇒売価変更データ異常テーブル:OK

テーブル定義書(売価変更企画IFテーブル).xls.⇒売価変更企画IFテーブル:OK

テーブル定義書(売価変更企画テーブル).xls.⇒売価変更企画テーブル:OK

テーブル定義書(売価変更企画明細テーブル).xls.⇒売価変更企画明細テーブル:OK

テーブル定義書(売価変更伝票元テーブル).xls.⇒売価変更伝票元テーブル:OK

テーブル定義書(売価変更修正一時テーブル).xls.⇒売価変更修正一時テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_baikahenkou_syusei_itiji, 28)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_baikahenkou_syusei_itiji, 28)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_baikahenkou_syusei_itiji, 28)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_baikahenkou_syusei_itiji, 28)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_baikahenkou_syusei_itiji, 28)。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。

テーブル定義書(売価変更全件IFテーブル).xls.⇒売価変更全件IFテーブル:OK

テーブル定義書(売価変更処分IFテーブル(閉鎖店)).xls.⇒売価変更処分IFテーブル(閉鎖店):OK

テーブル定義書(売価変更処分テーブル(閉鎖店)).xls.⇒売価変更処分テーブル(閉鎖店):OK

テーブル定義書(売場データIFテーブル).xls.⇒売場データIFテーブル:OK

テーブル定義書(売場台帳IFテーブル).xls.⇒売場台帳IFテーブル:OK
テーブル定義書(売場台帳IFテーブル).xls.⇒売場台帳送信ＩＦテーブル (2):OK

テーブル定義書(売場台帳バックアップテーブル).xls.⇒売場台帳バックアップテーブル:OK

テーブル定義書(売場台帳削除テーブル).xls.⇒売場台帳削除テーブル:OK

テーブル定義書(売場台帳削除不可テーブル).xls.⇒売場台帳削除不可テーブル:OK

テーブル定義書(売場台帳前日分テーブル).xls.⇒売場台帳前日分テーブル:OK

テーブル定義書(売場台帳前日分バックアップテーブル).xls.⇒売場台帳前日分バックアップテーブル:OK

テーブル定義書(売場台帳更新_仕入テーブル).xls.⇒売場台帳更新_仕入テーブル:OK

テーブル定義書(売場台帳更新_仮仕入テーブル).xls.⇒売場台帳更新_仮仕入テーブル:OK

テーブル定義書(売場台帳更新_値引IFテーブル).xls.⇒売場台帳更新_値引IFテーブル:OK

テーブル定義書(売場台帳更新_値引テーブル).xls.⇒売場台帳更新_値引テーブル:OK
テーブル定義書(売場台帳更新_値引テーブル).xls.⇒×売場台帳更新_値引テーブル (旧):OK

テーブル定義書(売場台帳更新_売価変更テーブル).xls.⇒売場台帳更新_売価変更テーブル:OK

テーブル定義書(売場台帳更新_店振入庫テーブル).xls.⇒売場台帳更新_店振入庫テーブル:OK

テーブル定義書(売場台帳更新_店振出庫テーブル).xls.⇒売場台帳更新_店振出庫テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_uriba_kousin_tenburi_syukko, 2)。
语句已终止。

テーブル定義書(売場台帳更新_減耗損テーブル).xls.⇒売場台帳更新_減耗損テーブル:OK

テーブル定義書(売場台帳更新_自家消費テーブル).xls.⇒売場台帳更新_自家消費テーブル:OK

テーブル定義書(売場台帳更新_販売テーブル).xls.⇒売場台帳更新_販売テーブル:OK

テーブル定義書(売場台帳更新_返品IFテーブル).xls.⇒売場台帳更新_返品IFテーブル:OK

テーブル定義書(売場台帳更新_返品テーブル).xls.⇒売場台帳更新_返品テーブル:OK
テーブル定義書(売場台帳更新_返品テーブル).xls.⇒売場台帳更新_返品テーブル (旧):OK

テーブル定義書(売場台帳更新エラーデータ).xls.⇒売場台帳更新エラーデータ:OK

テーブル定義書(売場台帳販売実績テーブル).xls.⇒売場台帳販売実績テーブル:OK

テーブル定義書(売場情報_原単価変更テーブル).xls.⇒売場情報_原単価変更テーブル:OK

テーブル定義書(売場情報_売価変更テーブル).xls.⇒売場情報_売価変更テーブル:OK

テーブル定義書(売場情報_大分類変更テーブル).xls.⇒売場情報_大分類変更テーブル:OK

テーブル定義書(売場情報_発注単位変更テーブル).xls.⇒売場情報_発注単位変更テーブル:OK

テーブル定義書(売場情報_発注単位変更結果テーブル).xls.⇒売場情報_発注単位変更結果テーブル:OK

テーブル定義書(売場情報_発注情報変更テーブル).xls.⇒売場情報_発注情報変更テーブル:OK

テーブル定義書(売場情報_発注情報変更履歴テーブル).xls.⇒売場情報_発注情報変更履歴テーブル:OK

テーブル定義書(売変理由IFテーブル).xls.⇒売変理由IFテーブル:OK

テーブル定義書(大分類データIFテーブル).xls.⇒大分類データIFテーブル:OK

テーブル定義書(大分類値引率テーブル(閉鎖店)).xls.⇒大分類値引率テーブル(閉鎖店):OK

テーブル定義書(大分類変更IFテーブル).xls.⇒大分類変更IFテーブル:OK

テーブル定義書(導入商品リストIFテーブル).xls.⇒導入商品リストIFテーブル:OK

テーブル定義書(導入商品リストテーブル).xls.⇒導入商品リストテーブル:OK

テーブル定義書(導入商品管理IFテーブル).xls.⇒導入商品管理IFテーブル:OK

テーブル定義書(導入商品管理テーブル).xls.⇒導入商品管理テーブル:OK

テーブル定義書(小分類データIFテーブル).xls.⇒小分類データIFテーブル:OK

テーブル定義書(帳票用_JAN変更テーブル).xls.⇒帳票用_JAN変更テーブル:OK

テーブル定義書(帳票用_売価変更テーブル).xls.⇒帳票用_売価変更テーブル:OK

テーブル定義書(店休日情報テーブル).xls.⇒店休日情報テーブル:OK

テーブル定義書(店別取引先マスタIFテーブル).xls.⇒店別取引先マスタIFテーブル:OK

テーブル定義書(店別情報IFテーブル).xls.⇒店別情報IFテーブル:OK

テーブル定義書(店振エラーデータテーブル).xls.⇒店振エラーデータテーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_tenburi_error, 1)。
语句已终止。

テーブル定義書(店振入力データテーブル).xls.⇒店振入力データテーブル:OK

テーブル定義書(店振入庫伝票テーブル).xls.⇒店振入庫伝票テーブル:OK

テーブル定義書(店振入庫伝票履歴テーブル).xls.⇒店振入庫伝票履歴テーブル:OK

テーブル定義書(店振出庫伝票テーブル).xls.⇒店振出庫伝票テーブル:OK

テーブル定義書(店振受信IFテーブル).xls.⇒店振受信IFテーブル:OK

テーブル定義書(店振受信テーブル).xls.⇒店振受信テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_tenburi_jyusin, 1)。
语句已终止。

テーブル定義書(店舗価格更新IFテーブル).xls.⇒店舗価格更新IFテーブル:OK

テーブル定義書(店舗価格更新テーブル).xls.⇒店舗価格更新テーブル:OK

テーブル定義書(店舗価格更新中間テーブル).xls.⇒店舗価格更新中間テーブル:OK

テーブル定義書(店舗切替情報).xls.⇒店舗切替情報:OK

テーブル定義書(店舗更新情報IFテーブル).xls.⇒店舗更新情報IFテーブル:OK

テーブル定義書(店舗更新情報テーブル).xls.⇒店舗更新情報テーブル:OK

テーブル定義書(循環棚卸スキャンテーブル).xls.⇒循環棚卸スキャンテーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_jyunkan_tanaorosi_scan, 3)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_jyunkan_tanaorosi_scan, 4)。
语句已终止。
语句已终止。

テーブル定義書(循環棚卸テーブル).xls.⇒循環棚卸テーブル:OK

テーブル定義書(抹消データIFテーブル).xls.⇒抹消データIFテーブル:OK

テーブル定義書(摘要データIFテーブル).xls.⇒摘要データIFテーブル:OK

テーブル定義書(操作証跡テーブル).xls.⇒操作証跡テーブル:OK

テーブル定義書(操作証跡履歴テーブル).xls.⇒操作証跡履歴テーブル:OK

テーブル定義書(日別実績テーブル).xls.⇒日別実績テーブル:OK

テーブル定義書(最終売価変更日付テーブル).xls.⇒最終売価変更日付テーブル:OK

テーブル定義書(月曜発注送信テーブル).xls.⇒月曜発注送信テーブル:OK

テーブル定義書(月販情報履歴).xls.⇒月販情報履歴:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(月販情報当月).xls.⇒月販情報当月:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(月販用売場台帳).xls.⇒月販用売場台帳:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(未納商品削除送信データIFテーブル).xls.⇒未納商品削除送信データIFテーブル:OK

テーブル定義書(未納明細削除データテーブル).xls.⇒未納明細削除データテーブル:OK

テーブル定義書(本社発注テーブル).xls.⇒本社発注テーブル:OK
テーブル定義書(本社発注テーブル).xls.⇒本社発注テーブル (旧):OK

テーブル定義書(本社発注受信IFテーブル).xls.⇒本社発注受信IFテーブル:OK

テーブル定義書(本社発注済テーブル).xls.⇒本社発注済テーブル:OK

テーブル定義書(本社発注送信IFテーブル).xls.⇒本社発注送信IFテーブル:OK

テーブル定義書(棚変更エラーチェックテーブル).xls.⇒棚変更エラーチェックテーブル:OK

テーブル定義書(棚変更テーブル).xls.⇒棚変更テーブル:OK

テーブル定義書(棚変更履歴テーブル).xls.⇒棚変更履歴テーブル:OK

テーブル定義書(検品エラーテーブル).xls.⇒検品エラーテーブル:OK

テーブル定義書(検品データテーブル).xls.⇒検品データテーブル:OK

テーブル定義書(検品仕入伝票計上印刷制御テーブル).xls.⇒検品仕入伝票計上印刷制御テーブル:OK

テーブル定義書(検品数量訂正印刷制御テーブル).xls.⇒検品数量訂正印刷制御テーブル:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_kenpinteisei_print, 5)。
语句已终止。

テーブル定義書(欠品候補リストテーブル).xls.⇒欠品候補リストテーブル:OK

テーブル定義書(欠品商品テーブル).xls.⇒欠品商品テーブル:OK

テーブル定義書(汎用マスタ変更IFテーブル).xls.⇒汎用マスタ変更IFテーブル:OK

テーブル定義書(決算棚卸エラーテーブル).xls.⇒決算棚卸エラーテーブル:OK

テーブル定義書(決算棚卸グループ品漏れデータIFテーブル).xls.⇒決算棚卸グループ品漏れデータIFテーブル:OK

テーブル定義書(決算棚卸グループ品漏れデータテーブル).xls.⇒決算棚卸グループ品漏れデータテーブル:OK

テーブル定義書(決算棚卸スキャンデータ).xls.⇒決算棚卸スキャンデータ:OK

テーブル定義書(決算棚卸ロックテーブル).xls.⇒決算棚卸ロックテーブル:OK

テーブル定義書(決算棚卸在庫更新エラーテーブル).xls.⇒決算棚卸在庫更新エラーテーブル:OK

テーブル定義書(決算棚卸実査データ_G品IFテーブル).xls.⇒決算棚卸実査データ_G品IFテーブル:OK

テーブル定義書(決算棚卸実査データ_単品IFテーブル).xls.⇒決算棚卸実査データ_単品IFテーブル:OK

テーブル定義書(決算棚卸実査データ_単品テーブル).xls.⇒決算棚卸実査データ_単品テーブル:OK

テーブル定義書(決算棚卸実査データ_Ｇ品テーブル).xls.⇒決算棚卸実査データ_Ｇ品テーブル:OK

テーブル定義書(決算棚卸差異データテーブル).xls.⇒決算棚卸差異データテーブル:OK

テーブル定義書(決算棚卸未スキャンテーブル).xls.⇒決算棚卸未スキャンテーブル:OK

テーブル定義書(決算棚卸未チェックリストデータテーブル).xls.⇒決算棚卸未チェックリストデータテーブル:OK

テーブル定義書(決算棚卸未棚卸データIFテーブル).xls.⇒決算棚卸未棚卸データIFテーブル:OK

テーブル定義書(決算棚卸未棚卸データテーブル).xls.⇒決算棚卸未棚卸データテーブル:OK

テーブル定義書(決算棚卸ＪＡＮコードマスタテーブル).xls.⇒決算棚卸ＪＡＮコードマスタテーブル:OK

テーブル定義書(消化仕入マスタIFテーブル).xls.⇒消化仕入マスタIFテーブル:OK

テーブル定義書(減耗損×自家消費理由コードIFテーブル).xls.⇒減耗損×自家消費理由コードIFテーブル:OK

テーブル定義書(減耗損データテーブル).xls.⇒減耗損データテーブル:OK

テーブル定義書(減耗損伝票テーブル).xls.⇒減耗損伝票テーブル:OK

テーブル定義書(特売予定マスタIFテーブル).xls.⇒特売予定マスタIFテーブル:OK

テーブル定義書(特売在庫データIFテーブル).xls.⇒特売在庫データIFテーブル:OK

テーブル定義書(特売実績テーブル).xls.⇒特売実績テーブル:OK

テーブル定義書(特売店在庫発注希望テーブル).xls.⇒特売店在庫発注希望テーブル:OK

テーブル定義書(特売店在庫発注希望受信IFテーブル).xls.⇒特売店在庫発注希望受信IFテーブル:OK

テーブル定義書(特売店在庫発注希望送信IFテーブル).xls.⇒特売店在庫発注希望送信IFテーブル:OK

テーブル定義書(特売情報IFテーブル).xls.⇒特売情報IFテーブル:OK

テーブル定義書(特売期間テーブル).xls.⇒特売期間テーブル:OK

テーブル定義書(特売発注数結果テーブル).xls.⇒特売発注数結果テーブル:OK

テーブル定義書(特売発注結果IFテーブル).xls.⇒特売発注結果IFテーブル:OK

テーブル定義書(現金明細入力テーブル).xls.⇒現金明細入力テーブル:OK

テーブル定義書(発注エラーIFテーブル).xls.⇒発注エラーIFテーブル:OK

テーブル定義書(発注エラーテーブル).xls.⇒発注エラーテーブル:OK

テーブル定義書(発注中止IFテーブル).xls.⇒発注中止IFテーブル:OK

テーブル定義書(発注中止在庫テーブル).xls.⇒発注中止在庫テーブル:OK

テーブル定義書(発注予定テーブル).xls.⇒発注予定テーブル:OK

テーブル定義書(発注予約テーブル).xls.⇒発注予約テーブル:OK

テーブル定義書(発注予約履歴テーブル).xls.⇒発注予約履歴テーブル:OK

テーブル定義書(発注予約送信テーブル).xls.⇒発注予約送信テーブル:OK

テーブル定義書(発注単位変更IFテーブル).xls.⇒発注単位変更IFテーブル:OK

テーブル定義書(発注単位変更全件IFテーブル).xls.⇒発注単位変更全件IFテーブル:OK

テーブル定義書(発注確定IFテーブル).xls.⇒発注確定IFテーブル:OK

テーブル定義書(発注確定データテーブル).xls.⇒発注確定データテーブル:OK

テーブル定義書(発注送信IFテーブル).xls.⇒発注送信IFテーブル:OK

テーブル定義書(発注送信テーブル).xls.⇒発注送信テーブル:OK

テーブル定義書(社員マスタIFテーブル).xls.⇒社員マスタIFテーブル:OK

テーブル定義書(納品予定データ(EDI)テーブル).xls.⇒納品予定データ(EDI)テーブル:OK

テーブル定義書(納品予定データ(手書き）テーブル).xls.⇒納品予定データ(手書き）テーブル:OK

テーブル定義書(納品予定データテーブル).xls.⇒納品予定データテーブル:OK

テーブル定義書(細目データIFテーブル).xls.⇒細目データIFテーブル:OK

テーブル定義書(締日データIFテーブル).xls.⇒締日データIFテーブル:OK

テーブル定義書(臨時更新用テーブル).xls.⇒臨時更新用テーブル:OK

テーブル定義書(自動売変バックアップテーブル(閉鎖店)).xls.⇒自動売変バックアップテーブル(閉鎖店):OK

テーブル定義書(自動売変処理日テーブル(閉鎖店)).xls.⇒自動売変処理日テーブル(閉鎖店):OK

テーブル定義書(自動売変対象商品テーブル(閉鎖店)).xls.⇒自動売変対象商品テーブル(閉鎖店):OK

テーブル定義書(自家消費一括取込エラーテーブル).xls.⇒自家消費一括取込エラーテーブル:OK

テーブル定義書(自家消費一括取込ワークテーブル).xls.⇒自家消費一括取込ワークテーブル:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(自家消費伝票テーブル).xls.⇒自家消費伝票テーブル:OK

テーブル定義書(荷受データテーブル(手書き）).xls.⇒荷受データテーブル(手書き）:OK

テーブル定義書(荷受データテーブル).xls.⇒荷受データテーブル:OK

テーブル定義書(複数アドレステーブル).xls.⇒複数アドレステーブル:OK
テーブル定義書(複数アドレステーブル).xls.⇒複数アドレステーブル (旧):OK

テーブル定義書(返品データIFテーブル).xls.⇒返品データIFテーブル:OK

テーブル定義書(返品データテーブル).xls.⇒返品データテーブル:OK

テーブル定義書(返品企画情報IFテーブル).xls.⇒返品企画情報IFテーブル:OK

テーブル定義書(返品企画情報テーブル).xls.⇒返品企画情報テーブル:OK

テーブル定義書(返品伝票テーブル).xls.⇒返品伝票テーブル:OK
テーブル定義書(返品伝票テーブル).xls.⇒返品伝票テーブル (旧):NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 1)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 2)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 3)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 4)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 5)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 6)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 7)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 8)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 10)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 11)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 12)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 13)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 14)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 15)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 16)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 17)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 19)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 20)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 22)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 23)。
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_henpin_denpyou, 24)。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。
语句已终止。

テーブル定義書(返品未処理IFテーブル).xls.⇒返品未処理IFテーブル:OK

テーブル定義書(返品理由IFテーブル).xls.⇒返品理由IFテーブル:OK

テーブル定義書(送信データ（ランク別欠品データ）).xls.⇒送信データ（ランク別欠品データ）:OK

テーブル定義書(送信データ（仕入）).xls.⇒送信データ（仕入）:OK

テーブル定義書(送信データ（仮仕入）).xls.⇒送信データ（仮仕入）:OK

テーブル定義書(送信データ（倉庫在庫）).xls.⇒送信データ（倉庫在庫）:OK

テーブル定義書(送信データ（売価変更）).xls.⇒送信データ（売価変更）:OK

テーブル定義書(送信データ（店振出庫）).xls.⇒送信データ（店振出庫）:OK

テーブル定義書(送信データ（欠品調査リスト）).xls.⇒送信データ（欠品調査リスト）:OK

テーブル定義書(送信データ（減耗損）).xls.⇒送信データ（減耗損）:OK

テーブル定義書(送信データ（発注中止在庫）).xls.⇒送信データ（発注中止在庫）:OK

テーブル定義書(送信データ（自家消費）).xls.⇒送信データ（自家消費）:OK

テーブル定義書(週別実績テーブル).xls.⇒週別実績テーブル:OK
テーブル定義書(週別実績テーブル).xls.⇒×週別実績テーブル (OLD):OK

テーブル定義書(過去税率売上返品データ).xls.⇒過去税率売上返品データ:NG
违反了 PRIMARY KEY 约束“PK_t_table_info”。不能在对象“dbo.t_table_info”中插入重复键。重复键值为 (tbxDataSource, tbxDbName, t_kakozeiritu_uriage_henpin, 203)。
语句已终止。

テーブル定義書(部門別予算テーブル).xls.⇒部門別予算テーブル:OK

テーブル定義書(部門別予算金額IFテーブル).xls.⇒部門別予算金額IFテーブル:OK

テーブル定義書(部門別予算金額テーブル).xls.⇒部門別予算金額テーブル:OK

テーブル定義書(部門別売上精算テーブル).xls.⇒部門別売上精算テーブル:OK

テーブル定義書(部門清算売上IFテーブル).xls.⇒部門清算売上IFテーブル:OK

テーブル定義書(陳列量変更一括登録エラーテーブル).xls.⇒陳列量変更一括登録エラーテーブル:NG
从数据类型 varchar 转换为 numeric 时出错。

テーブル定義書(陳列量変更一括登録ワークテーブル).xls.⇒陳列量変更一括登録ワークテーブル:OK

テーブル定義書(Ｇ品異常原価率IFテーブル).xls.⇒Ｇ品異常原価率IFテーブル:OK

テーブル定義書(Ｇ品異常原価率データテーブル).xls.⇒Ｇ品異常原価率データテーブル:OK

